import turtle
import time

time.sleep(3) # Detener la tortuga por 3 segundos para ver la posición

turtle.forward(150) # Avanzar la tortuga por 250 unidades
time.sleep(1) # Detener la tortuga por 1 segundo para ver la posición

turtle.left(90) # Girar la tortuga 90 grados a la izquierda
time.sleep(1) # Detener la tortuga por 1 segundo para ver la posición

turtle.forward(50) # Avanzar la tortuga por 250 unidades
time.sleep(1) # Detener la tortuga por 1 segundo para ver la posición

turtle.left(90) # Girar la tortuga 90 grados a la izquierda
time.sleep(1) # Detener la tortuga por 1 segundo para ver la posición

turtle.forward(150) # Avanzar la tortuga por 250 unidades
time.sleep(1) # Detener la tortuga por 1 segundo para ver la posición

turtle.left(90) # Girar la tortuga 90 grados a la izquierda
time.sleep(1) # Detener la tortuga por 1 segundo para ver la posición

turtle.forward(50) # Avanzar la tortuga por 250 unidades
time.sleep(1) # Detener la tortuga por 1 segundo para ver la posición

turtle.left(90) # Girar la tortuga 90 grados a la izquierda
time.sleep(5) # Detener la tortuga por 5 segundos para ver la posición